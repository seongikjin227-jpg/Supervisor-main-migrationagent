{
  "rules": [
    {
      "rule_id": "GENERAL_ANSI_JOIN",
      "rule_type": "GENERAL",
      "category": "general",
      "priority": "mandatory",
      "guidance": [
        "Rewrite legacy comma joins and Oracle outer join operators (+) into explicit ANSI JOIN syntax.",
        "Keep the original join semantics, including inner joins, left/right outer joins, join predicates, filters, aliases, and bind placeholders."
      ]
    }
  ]
}
